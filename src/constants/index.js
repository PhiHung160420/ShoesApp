import theme, {
  COLORS,
  SIZES,
  darkTheme,
  lightTheme,
  selectedTheme,
} from './theme';

import icons from './icons';

export {theme, icons, COLORS, SIZES, darkTheme, lightTheme, selectedTheme};
